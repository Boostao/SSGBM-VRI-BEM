 There is some data that I still need to put together for you for some of the below steps.

- [ ] Add a Terrain Roughness Index aggregate to polygons
    - This should be fairly straightforward, using the DEM we typically use for slope, aspect, and elevation
- [ ] Update the SkWERM Resource Ratings Models to output Resource Suitability Index values for each ecosystem, including weighted average and highest value for each polygon, in addition to Wildlife Habitat Ratings
    - When assigning RRM values back to the spatial data set (merge_rrm_on_vri), we currently only keep the rating values (ending in _6C)
    - We’d like to also keep the RSI values (“_RSI”) generated by the rrm_calc_ratings process
    - Currently we calculate weighted average (“_WA”) with the rating values. Instead, we’d like to first calculate weighted average using RSI values (weighted by decile) and then convert that to the rating value weighted average using the predetermined breaks
    - The end result would be:
        - All of the currently existing suitability and capability rating variables (_6C) for each ecosystem (SU_1, SU_2, SU_3, CAP_1, CAP_2, CAP_3) and highest value (HV) calculated from them
        - Associated suitability and capability RSI values (_RSI) for each ecosystem (SU_1, SU_2, SU_3, CAP_1, CAP_2, CAP_3), weighted average RSI, and weighted average rating variables (_6C_SU_WA and _6C_SU_CAP)
        - In cases where an RSI value is missing for the most common ecosystem (SU_1/CAP_1), even if there are values for the secondary or tertiary ecosystems, weighted average should be NA
            - It is okay to calculate weighted average if values are only missing from the second and third ecosystems (SU_2/CAP_2 and SU_3/CAP_3)
- [ ] Convert the vector-based SkWERM model process to raster (tested as a separate process first; this may resolve the issue with slivers)
    - We’ve had issues with accuracy because values from raster datasets (like slope and aspect) are summarized across a polygon. Converting to a raster-based process would solve this
        - Some of the process may still need to be vector-based when it comes to combining polygon layers
    - What we’d like as output:
        - Rasters of all of the wildlife habitat ratings
        - Other environmental information (the fields generated in clean_vri_bem_output): because of the number of fields, I’m not sure if it’s better to present this as a raster or vector
    - There’s flexibility in how this process looks, but the RRM calculation should use rasters (to avoid spatially aggregated data affecting the final ratings) and the wildlife habitat ratings/RSI output should be rasters
    - I attached an example of how we currently run the model (SkWERM_run_example); this is not runnable code but just shows the order in which we run things currently
        - We now use a forest disturbance layer instead of the burn and consolidated cutblock layers (for calc_forest_age_class). It’s not currently publicly available and I’m not sure if I’m allowed to share it, so for now you can use the consolidated cutblock layer and make the following adjustments:
        - MRSRD_Y =  ccb$HARVEST_YEAR
        - I don’t think you’ll need these fields from the forest disturbance layer, but if you do:
            - MRSRD_A = “CUT”
            - MRSRD_D = “CON”
            - MRSRD_S = “N”
            - SIFA = current year – MRSRD_Y
            - DSTRB_HIST = paste(MRSRD_Y,MRSRD_A,MRSRD_S,MRSRD_D,sep= “_”)
- [ ] Merge small polygon slivers (weighted by ecosystem similarity)
        - We’d like a minimum polygon size of 1000 m2 at the end of the process. 
            - For anything smaller, could you merge it into the nearest polygon, weighted by similarity between BGC_ZONE, BGC_SUBZON, BGC_VRT, BGC_PHASE, and BEUMC_S1?
            - The raster process may address this issue
- [ ] Convert the SkWERM process to use Predictive Ecosystem Mapping (PEM) instead of Broad Ecosystem Mapping (BEM) – test with Deception Lake AOI
    - This is meant to be a pilot and should be kept separate from the current SkWERM process. We intend to transition to the PEM once it becomes more widely available in our area
    - The PEM layer will be available as both a vector and a raster. Please use whichever you prefer for the process
        - The PEM ecosystem value is the “.pred_class” field in the vector data set. If the raster is used, the raster value is associated with map.response in our response key, which can be used to map the ecosystem label back to .pred_class
    - In some cases, the PEM label is simply “non-forested”. In those cases we’d still want BEM values (BEUMC_S1) assigned
        - For example, a field “Ecosystem” may have either the PEM values (e.g., .pred_model) values or BEUMC_S1 values in cases where the .pred_model values are non-forested
        - We will also need SITE_M3A values from the BEM
    - The BEM has three deciles and associated information; the PEM has a single ecosystem label
        - This means that we won’t need to break things up into threes (e.g., it will be CROWN_ALL, not CROWN_ALL_1, CROWN_ALL_2, CROWN_ALL_3) and there will not be a weighted average (WA) or highest value (HV) rating
    - We will need to provide updated RRM values and input tables to complete the process
        - To do this, we’ll first need to calculate site series forage prominence values. It may be a bit before we have these values ready
- [ ] Validate the PEM / BEM conversion with site series forage prominence values (AOI)
    - We will need to calculate site series forage prominence values, so will provide those in the future

- [ ] Draft R markdown or Quatro output to speed up referral process (read in geomark AOI, read in spatial layers of interest, summarize overlaps in output report, provide rundown of how to make updates between R and Quarto)

    - This is a separate project from SSGBM
        - We will need a new (private) Git repository for this. I wasn’t around when the SSGBM-VRI-BEM repository was created; I’m guessing we should make the repository and invite you to edit?
    - For this process, we will start with either a polygon or line spatial feature (the geomark AOI). We have around 100 reference layers (point, polygon, lines, and raster) where we need to know:
        - If the geomark AOI is a polygon:
            - If it overlaps with a polygon reference layer, what is the percent overlap?
            - If it overlaps with a raster reference layer, what is the percent overlap of each raster value?
            - If there are linear features (e.g., roads, streams) within the polygon, what is the density and type?
            - If there are points within it, how many and what type?
            - If there are points or polygons within 1 km of the geomark, which are they and how far away are they?
        - If the geomark AOI is a line:
            - If it overlaps with a polygon or raster reference layer, what length of the linear feature is overlapping?
            - If it intersects with a linear feature, what is the ID of the feature?
            - If there are points or polygons within 1 km of the geomark, which are they and how far away are they?
        - If the geomark AOI does not intersect with a layer, no information needs to be reported from that layer (it can effectively be ignored). For cases where it does intersect, there are fields that will need to be extracted from the reference layer in addition to % overlap (which will change based on the reference layer)
            - For example, if it falls within a provincial fish range, we’ll need to know what species of fish it is
        - I’m working on putting together a list of the reference layers (I won’t finish it by the end of today, and will need to work on it the week of March 23 before I can send it to you)
- [ ] Improve handling of large spatial datasets using DuckDB spatial to avoid crashes: (https://github.com/Boostao/SSGBM-VRI-BEM/tree/DuckDB_refactor)
    - You’ve already made improvements to the handling of large spatial datasets—we left this in here in case you had any further tweaks you wanted to make
 

I added some documents to Sharepoint:

    - I created a “PEM” folder with the raster PEM, vector PEM, and PEM response key
    - In the RRM/RRM_inputs folder, I added RRM templates for moose growing season shelter (GFD) and grizzly bear early spring (MURAR_PEFD_WHR_SSGBM_template), late spring (MURAR_PLFD_WHR_SSGBM_template), summer (MURAR_SFD_WHR_SSGBM_template), and fall forage (MURAR_FFD_WHR_SSGBM_template), as well as hibernation (MURAR_HI_WHR_SSGBM_template)
        - The Skeena_GBC_BEU_Rating sheet in each of the grizzly bear templates has the RSI values
        - The “RSI_SOURCE of Ratings for Initial Attributes Tab in Models_MALAN_Skeena_15May2023” has the RSI values for moose
        - These are all of the RRM values that we generate with the model. You may not need to use all of these, I just wanted to give you options
    - I still need to give you:
        - Information about the referral data layers (source and what information we would need from each layer)
        - Updated RSI values for running the PEM (the current RSI values are made for the BEM and won’t work with the PEM)