% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/merge_ccb_duckdb.R
\name{merge_geometry_duckdb}
\alias{merge_geometry_duckdb}
\title{Spatial union-split of two polygon tables inside DuckDB}
\usage{
merge_geometry_duckdb(conn, x_tbl, y_tbl, tolerance_m2 = 10, result_tbl)
}
\arguments{
\item{conn}{A \code{duckdb_connection} object (as returned by \code{\link[=init_conn]{init_conn()}}).}

\item{x_tbl}{Character. Name of the base table / view in \code{conn} (e.g.
the VRI-BEM polygons).  Must contain a \code{Shape} geometry column.}

\item{y_tbl}{Character. Name of the overlay table / view in \code{conn} (e.g.
CCB, burn perimeters).  Must contain a \code{Shape} geometry column.}

\item{tolerance_m2}{Numeric. Minimum polygon area (m²) to retain.
Fragments smaller than or equal to this value are dropped.
Default \code{10}.}

\item{result_tbl}{Character. Name of the (temp) table written to \code{conn}
containing the final result.}
}
\value{
The name of the result table (\code{result_tbl}) invisibly.
}
\description{
This is the DuckDB-native counterpart of \code{\link[=merge_geometry]{merge_geometry()}}.
All geometry work (intersection, difference, area filtering) happens
entirely inside the database via DuckDB Spatial — no R sf objects are
created.
}
\details{
The operation is equivalent to a spatial union:
\itemize{
\item \code{x} polygons that overlap \code{y} polygons are split into
(a) the intersection piece (carries all \code{y} attributes), and
(b) the remaining difference piece (\code{y} attributes are \code{NULL}).
\item \code{x} polygons with no \code{y} overlap are passed through unchanged
(\code{y} attributes are \code{NULL}).
\item Any resulting polygon whose area is ≤ \code{tolerance_m2} is discarded.
\item Geometry collections are reduced to plain \code{POLYGON} geometry via
\code{\link[=sanitize_geometry_duckdb]{sanitize_geometry_duckdb()}}.
}
}
