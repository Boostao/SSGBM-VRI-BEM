% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/correct_small_lakes_duckdb.R
\name{correct_small_lakes_duckdb}
\alias{correct_small_lakes_duckdb}
\title{Correct small lakes in a DuckDB VRI-BEM table}
\usage{
correct_small_lakes_duckdb(
  conn,
  vri_bem_tbl = "VRIBEM",
  lakes_tbl = "V_LAKES",
  tolerance_m2 = 10,
  batch_size = 500L,
  mem_limit = "6GB",
  result_tbl = "VRIBEM"
)
}
\arguments{
\item{conn}{A \code{duckdb_connection} object (as returned by \code{\link[=init_conn]{init_conn()}}).}

\item{vri_bem_tbl}{Character. Name of the source VRI-BEM table in \code{conn}.
Default \code{"VRIBEM_CORRECTIONS"}.}

\item{lakes_tbl}{Character. Name of the lakes table/view in \code{conn}.
Default \code{"V_LAKES"}.}

\item{tolerance_m2}{Numeric. Minimum polygon area (m²) to retain after
splitting.  Default \code{10}.}

\item{batch_size}{Integer. Number of non-lake VRI rows to process per
iteration when computing intersections and differences.  Smaller values
use less peak memory at the cost of more round-trips; larger values are
faster but require more RAM.  Default \code{500L}.}

\item{mem_limit}{Character. DuckDB memory limit string (e.g. \code{"6GB"}) to
use as a floor for the spatial batch operations. If the current connection
limit is already higher, it is left unchanged. By the time this function is
called the buffer pool may be near-full from prior steps (\code{V_VRI}, \code{VRIBEM},
etc.), leaving no headroom for even a small \code{ST_Intersection} allocation.
Raising the limit lets DuckDB evict clean (checkpointed) pages from the
\code{V_} tables on demand. Restored to the original value when the function
returns. Default \code{"6GB"}.}

\item{result_tbl}{Character. Name of the (temp) table written to \code{conn}
with the corrected result.  Defaults to \code{"VRIBEM_CORRECTIONS"}, which
replaces the input table in-place.}
}
\value{
The name of the result table (\code{result_tbl}) invisibly.
}
\description{
DuckDB-native counterpart of \code{\link[=correct_small_lakes]{correct_small_lakes()}}.
Polygons whose \code{BEUMC_S1} is not already a lake code (\code{LS}, \code{LL}, \code{OW})
are split against the FWA Lakes layer:
\itemize{
\item \strong{Intersection pieces} receive an updated \code{BEUMC_S1} (\code{OW} for
areas < 10 ha, \code{LS} otherwise), \verb{BCLCS_LV_*} overrides, and cleared
\verb{SPEC_CD_*} / \verb{SPEC_PCT_*} columns.
\item \strong{Difference pieces} (residual non-lake area) keep their original
attributes; tiny slivers below \code{tolerance_m2} are discarded.
\item \strong{Already-classified lake polygons} pass through unchanged.
}
After recombining, all rows where \verb{BEUMC_S1 IN ('LS','LL','OW')} receive
\code{SDEC_1 = 10}, \code{SDEC_2 = 0}, \code{SDEC_3 = 0}, and \code{SITE_M3A = NULL} — matching
the clean-up performed at the end of \code{\link[=correct_small_lakes]{correct_small_lakes()}}.
}
