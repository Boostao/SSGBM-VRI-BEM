% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/merge_elevation_duckdb.R
\name{merge_elevation_duckdb}
\alias{merge_elevation_duckdb}
\title{Compute slope, aspect, elevation and their derivatives inside DuckDB}
\usage{
merge_elevation_duckdb(
  conn,
  vri_bem_tbl,
  elev_raster,
  elevation_threshold = 1500,
  terrain_raster = NULL,
  result_tbl = "VRIBEM_ELEVATION"
)
}
\arguments{
\item{conn}{A \code{duckdb_connection} object (as returned by \code{\link[=init_conn]{init_conn()}}).}

\item{vri_bem_tbl}{Character. Name of the table / view in \code{conn} that holds
the VRI-BEM polygons.  The table \strong{must} contain a \code{Shape} geometry
column and the fields \code{BEUMC_S1}, \code{BEUMC_S2}, \code{BEUMC_S3} and \code{BGC_ZONE}.}

\item{elev_raster}{\code{SpatRaster} representing the elevation model.}

\item{elevation_threshold}{Numeric. Elevation (m) above which
\code{ABOVE_ELEV_THOLD} is set to \code{"Y"}. Default \code{1500}.}

\item{terrain_raster}{Optional \code{SpatRaster} with layers \code{slope} and
\code{aspect} expressed in \strong{radians}.  When \code{NULL} (default) the terrain
raster is derived from \code{elev_raster} via \code{\link[terra:terrain]{terra::terrain()}}.}

\item{result_tbl}{Character. Name given to the (temp) table written to
\code{conn} that will hold the final result including the new columns.
Defaults to \code{"VRIBEM_ELEVATION"}.}
}
\value{
The name of the result table (\code{result_tbl}) invisibly.  The caller
can query \code{conn} directly, e.g. \code{duckdb::dbReadTable(conn, result_tbl)}.
}
\description{
This is the DuckDB-optimised counterpart of \code{\link[=merge_elevation_raster_on_sf]{merge_elevation_raster_on_sf()}}.
Raster cells are exported to temporary \strong{Parquet files on disk} via
\code{\link[arrow:write_parquet]{arrow::write_parquet()}}.  DuckDB then reads those files natively with
\code{read_parquet()} and performs the spatial join (\code{ST_Contains(polygon, ST_Point(x, y))}), aggregation, and \code{SLOPE_MOD} / \code{ABOVE_ELEV_THOLD}
derivation entirely in SQL — R never holds the raster as an in-memory vector
object.
}
\details{
\strong{Memory model}: each raster layer is converted to a flat \verb{(x, y, value)}
data frame via \code{\link[terra:as.data.frame]{terra::as.data.frame()}} and written to a temp Parquet file.
DuckDB streams those files directly from disk via \code{read_parquet()}, so the
peak R memory is one data frame at a time rather than three full
\code{SpatVector} polygon objects.

\strong{SLOPE_MOD} derivation rules (identical to \code{\link[=merge_elevation_raster_on_sf]{merge_elevation_raster_on_sf()}}):
\itemize{
\item \code{j} - gentle slope: 10-25 \% (non CWH/MH) or 10-35 \% (CWH/MH)
\item \code{k} - cool aspect (285-359 or 0-134 deg) AND moderate slope
\item \code{q} - very steep cool aspect (slope > 100 \%)
\item \code{w} - warm aspect (135-284 deg) AND moderate slope
\item \code{z} - very steep warm aspect (slope > 100 \%)
}

Polygons whose \code{BEUMC_S1}, \code{BEUMC_S2}, or \code{BEUMC_S3} belongs to the
\emph{no-slope-mod} list (water / non-terrestrial codes) receive \code{NULL}.
}
