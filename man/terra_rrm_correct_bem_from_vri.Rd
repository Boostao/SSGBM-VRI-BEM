% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/terra_rrm_corrections.R
\name{terra_rrm_correct_bem_from_vri}
\alias{terra_rrm_correct_bem_from_vri}
\title{Terra-native VRI-driven BEM corrections}
\usage{
terra_rrm_correct_bem_from_vri(
  x,
  clear_site_ma = TRUE,
  use_ifelse = TRUE,
  beu_bec = NULL,
  filename = NULL,
  overwrite = FALSE
)
}
\arguments{
\item{x}{A \code{terra::SpatRaster} containing aligned VRI/BEM layers.}

\item{clear_site_ma}{Logical. If \code{TRUE}, \code{SITE_M1A}, \code{SITE_M2A}, and
\code{SITE_M3A} are cleared at the start of the stage to match the current
workflow.}

\item{use_ifelse}{Logical. Preserves the current duplicate-BEUMC behaviour
where those rows are marked updated immediately.}

\item{beu_bec}{Optional data frame containing the allowed BEC/BEU lookup used
for the final per-component correction pass.}

\item{filename}{Optional filename for writing the corrected stack.}

\item{overwrite}{Logical passed to terra writes.}
}
\value{
A \code{SpatRaster} representing the corrected stack.
}
\description{
Builds the ordered on-disk VRI correction stage for the raster-only path.
This stage covers duplicate component consolidation, map-code reassignment,
removal of inappropriate wetland components in treed units, stand updates,
and allowed BEC/BEU correction lookups.
}
\details{
All heavy computation runs in a single \code{terra::blocks()} read/write pass
over the raster. No lazy terra DAG is accumulated across rules, which keeps
memory usage and I/O proportional to 2× file size regardless of rule count.
}
