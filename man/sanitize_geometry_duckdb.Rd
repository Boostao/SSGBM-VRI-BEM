% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/merge_ccb_duckdb.R
\name{sanitize_geometry_duckdb}
\alias{sanitize_geometry_duckdb}
\title{Sanitize geometry in a DuckDB table or view}
\usage{
sanitize_geometry_duckdb(conn, input_tbl, result_tbl, tolerance_m2 = 10)
}
\arguments{
\item{conn}{A \code{duckdb_connection} object (as returned by \code{\link[=init_conn]{init_conn()}}).}

\item{input_tbl}{Character. Name of the source table or view in \code{conn}.
Must contain a \code{Shape} geometry column.}

\item{result_tbl}{Character. Name of the temporary table to create.}

\item{tolerance_m2}{Numeric. Minimum polygon area (m²) to retain.
Rows whose sanitized area is ≤ this value are dropped.  Default \code{10}.}
}
\value{
The name of the result table (\code{result_tbl}) invisibly.
}
\description{
Applies the DuckDB-native equivalent of \code{\link[=sanitize_geometry]{sanitize_geometry()}}:
\enumerate{
\item Applies \code{ST_MakeValid} to the \code{Shape} column.
\item Strips non-polygon sub-geometries via \code{ST_CollectionExtract(..., 3)}.
\item Drops rows whose resulting polygon area is ≤ \code{tolerance_m2}.
}
}
\details{
The result is written as a new temporary table named \code{result_tbl}.  Any
existing table with that name is replaced.  All non-geometry columns are
carried over unchanged via \verb{* EXCLUDE (Shape)}.
}
