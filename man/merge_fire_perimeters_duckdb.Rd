% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/merge_fire_perimeters_duckdb.R
\name{merge_fire_perimeters_duckdb}
\alias{merge_fire_perimeters_duckdb}
\title{Merge fire perimeter attributes into a DuckDB VRI-BEM table}
\usage{
merge_fire_perimeters_duckdb(
  conn,
  vri_bem_tbl = "VRIBEM_CCB",
  fire_tbl = "V_FIRE"
)
}
\arguments{
\item{conn}{A \code{duckdb_connection} object (as returned by \code{\link[=init_conn]{init_conn()}}).}

\item{vri_bem_tbl}{Character. Name of the VRI-BEM table to modify in-place.
Default \code{"VRIBEM_CCB"}.}

\item{fire_tbl}{Character. Name of the fire perimeters table/view in \code{conn}.
Must contain a \code{Shape} geometry column and a \code{FIRE_YEAR} integer column.
Default \code{"V_FIRE"}.}
}
\value{
The name of \code{vri_bem_tbl} invisibly.
}
\description{
DuckDB-native counterpart of \code{\link[=merge_fire_perimeters]{merge_fire_perimeters()}}.
Adds two columns to \code{vri_bem_tbl} \strong{in-place}:
\describe{
\item{\code{percent_burned}}{Percent of the polygon area overlapping the most
recent fire perimeter that intersects it. \code{0} when no overlap, or when
\code{BEUMC_S1} is a water code (\code{OW}, \code{LL}, \code{LS}).}
\item{\code{most_recent_fire}}{\code{FIRE_YEAR} of the most recent fire
intersecting the polygon. \code{NULL} when no overlap or for water polygons.}
}
}
\details{
Logic matches \code{\link[=merge_fire_perimeters]{merge_fire_perimeters()}} exactly:
\enumerate{
\item For each (VRI polygon × fire polygon) pair the intersection area is
computed and grouped by \verb{(polygon rowid, FIRE_YEAR)}.
\item The row with the maximum \code{FIRE_YEAR} per polygon is kept (using
the DuckDB \code{QUALIFY} clause).
\item \code{percent_burned} is derived as
\code{intersect_area / ST_Area(polygon) * 100}.
\item Polygons with no fire overlap receive \code{percent_burned = 0} and
\code{most_recent_fire = NULL} (the column default).
\item Water polygons (\verb{BEUMC_S1 IN ('OW','LL','LS')}) are reset to
\code{percent_burned = 0}, \code{most_recent_fire = NULL}.
}
}
