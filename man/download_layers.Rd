% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download_layers.R
\name{download_layers}
\alias{download_layers}
\alias{download_db_layers}
\alias{download_vri}
\alias{download_bem}
\alias{download_wetlands}
\alias{download_rivers}
\alias{download_lakes}
\alias{download_glaciers}
\alias{download_ccb}
\alias{download_burn}
\alias{download_fire}
\alias{download_pem}
\alias{download_tsa}
\alias{download_skeena}
\title{Download and materialize vector layers for rasterization}
\usage{
download_db_layers(
  path,
  bem_dsn = NULL,
  prem_dsn = NULL,
  tsa_dsn = NULL,
  tsa_name = c(),
  Skeena_boundary = TRUE,
  overwrite = FALSE
)

download_vri(path, dsn = NULL, layer = "fc", overwrite = FALSE)

download_bem(path, dsn, layer = "BEM", overwrite = FALSE)

download_wetlands(
  path,
  dsn = NULL,
  layer = "FWA_WETLANDS_POLY",
  overwrite = FALSE
)

download_rivers(path, dsn = NULL, layer = "FWA_RIVERS_POLY", overwrite = FALSE)

download_lakes(path, dsn = NULL, layer = "FWA_LAKES_POLY", overwrite = FALSE)

download_glaciers(path, dsn = NULL, layer = "BTM_PLU_V1", overwrite = FALSE)

download_ccb(path, dsn = NULL, layer = "CNS_CUT_BL_polygon", overwrite = FALSE)

download_burn(
  path,
  dsn = NULL,
  layer = "WHSE_FOREST_VEGETATION_VEG_BURN_SEVERITY_SP",
  overwrite = FALSE
)

download_fire(
  path,
  dsn = NULL,
  layer = "WHSE_LAND_AND_NATURAL_RESOURCE.PROT_HISTORICAL_FIRE_POLYS_SP",
  overwrite = FALSE
)

download_pem(path, dsn, layer = "PEM_Mar2026", overwrite = FALSE)

download_tsa(path, tsa_name = c(), tsa_dsn = NULL, overwrite = FALSE)

download_skeena(path, overwrite = FALSE)
}
\arguments{
\item{path}{Output datasource path or output directory.}

\item{bem_dsn}{Source datasource for BEM.}

\item{prem_dsn}{Source datasource for PEM.}

\item{tsa_dsn}{Source datasource for TSA.}

\item{tsa_name}{Character vector of TSA names used to filter the TSA layer.}

\item{Skeena_boundary}{Logical. Also materialize the Skeena region boundary.}

\item{overwrite}{Logical. Overwrite an existing output datasource.}

\item{dsn}{Optional source datasource. When left \code{NULL}, supported layers are
fetched from the BC Data Catalog.}

\item{layer}{Source layer name.}
}
\value{
Character path to the written datasource. \code{download_db_layers()}
returns a named list of datasource paths.
}
\description{
These helpers save the vector inputs used by \code{\link[=init_db]{init_db()}} to durable files on
disk so they can be rasterized later in the terra pipeline without keeping
them in DuckDB tables.
}
\details{
When \code{path} is a directory, the helper writes a default GeoPackage file for
the requested layer into that directory. When \code{path} is a file path, that
path is used directly.

Query-backed downloads are written as a single-layer GeoPackage. When a
source datasource is supplied explicitly, that datasource is materialized
with the same transformed fields used by the package readers.
}
